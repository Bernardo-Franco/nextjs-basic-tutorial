import { Rubik, Lusitana } from 'next/font/google';

export const inter = Rubik({ weight: '400', subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
